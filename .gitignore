# .NET and IDE output
[Bb]in/
[Oo]bj/
.vs/
TestResults/
artifacts/
*.user
*.suo
*.nupkg
.clinerules
.hermes

# Browser extension output
node_modules/
dist/
coverage/
*.tsbuildinfo

# ASP.NET Data Protection key ring (secret material, never commit)
.dataprotection-keys/

# Local TLS certificates (mkcert LAN cert/key, never commit)
/certs/

# Local runtime data, backups, and secrets
# Anchored to repo root so it does not also match src/BookmarkManager.Api/Data/
# (case-insensitive on Windows), which is real source code.
/data/
backups/
*.db
*.db-shm
*.db-wal
*.log
.env
.env.*
!.env.example

# AI assistants — local tool state only (canonical knowledge is tracked under .agents/)
/setup.md
/AGENT.md
.codex/
.codegraph/
.claude/
.cursor/
.continue/
.windsurf/
.roo/
.cline/
.opencode/
.gemini/
/GEMINI.md
.aider*
kickbacks.vsix
/implementation.md
.hermes

# Shared agent knowledge is TRACKED — do not ignore:
#   .agents/
#   AGENTS.md
#   CLAUDE.md
# After clone: pwsh -File scripts/setup-agent-links.ps1

# Playwright MCP debug dumps (console logs, page snapshots)
.playwright-mcp/

calendar.md
skills-lock.json

# graphify knowledge graph index (local, regenerable via /graphify --update)
graphify-out/